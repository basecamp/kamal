service: app
image: dhh/app
servers:
  web:
    - "1.1.1.1"
    - "1.1.1.2"
registry:
  server: localhost:5000

builder:
  arch: <%= Kamal::Utils.docker_arch == "arm64" ? "amd64" : "arm64" %>
  remote: ssh://app@1.1.1.5:2222
