#!/bin/sh

bin/kamal server exec "
  ufw allow ssh && \
  ufw allow http && \
  ufw allow https && \
  ufw --force enable && \
  apt-get install fail2ban -y && \
  systemctl start fail2ban && \
  systemctl enable fail2ban"
